Rewrite the following request to be unambiguous using the Grammar of a
Complete Request (role, context, material, verb, constraints, format,
verification, epistemic — see grammar.md if present in this project).

Rules:
- Fill each slot from what the request actually states or the conversation
  establishes.
- Mark every slot you had to GUESS with [ASSUMED] and state the assumption.
- Leave a slot as "(not needed)" when the task genuinely doesn't require it
  — simple asks should stay simple.
- Show me the rewrite and WAIT for my confirmation before executing
  anything.

$ARGUMENTS
